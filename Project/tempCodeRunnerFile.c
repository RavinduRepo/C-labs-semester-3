l] : -1;// start to portal 1
    // StoT1 = (StoT1 > 0 && Road[TR1][TC1] != -1)? StoT1 : -1 * StoT1; //multiplying with - if already -

    // int StoT2 = (Road[TR2][TC2] != -1)? Road[TR2][TC2] - Road[StartRow][StartCol] : -1;// start to portal 2
    // StoT2 = (StoT2 > 0 && Road[TR2][TC2] != -1)? StoT2 : -1 * StoT2; //multiplying with - if already 